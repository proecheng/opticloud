import { SecurityDisclosurePageView } from "./SecurityDisclosurePageView";

export const metadata = {
  title: "Security Disclosure — OptiCloud",
  description: "Responsible vulnerability disclosure contact, report requirements, SLA, and safe harbor boundaries.",
};

export default function SecurityDisclosurePage(): JSX.Element {
  return <SecurityDisclosurePageView />;
}
